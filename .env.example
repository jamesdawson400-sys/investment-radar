# Local development (SQLite — zero setup)
DATABASE_URL="file:./dev.db"

# Production on Vercel (replace with your Vercel Postgres / Neon connection string)
# DATABASE_URL="postgresql://user:password@host:5432/investment_radar?sslmode=require"
